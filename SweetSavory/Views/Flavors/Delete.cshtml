@{
  Layout = "_Layout";
}

@using SweetSavory.Models
@using System.Security.Claims
@model SweetSavory.Models.Flavor

<br>
<br>
@if(User.Identity.IsAuthenticated)
{
  <div class="card">
    <h4>Are you sure you want to delete this?</h4>
    <hr>
    <h5>@Html.DisplayName("Flavor Name"): @Html.DisplayFor(model => model.FlavorName)</h5>
    <br>
    @using (Html.BeginForm())
    {
      <input type="submit" value="Delete" class = "btn btn-outline-info" />
    }
  </div>  
}
else
{
  <h4>You are not authorized to Delete this Flavor.</h4>
}    
<br>
@Html.ActionLink("Back to List", "Index", "Flavors", null, new { @class = "btn btn-outline-info"})